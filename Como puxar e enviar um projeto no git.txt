cd "chegar em uma pasta"
cd / "voltar todas as pastas"
cd .. "voltar uma pasta"

cd users
cd andre
cd desktop
git clone https://github.com/AndreCrimsom/Projeto-Consultas-M-dicas "pode ser o link de qualquer repositório"
cd projeto-consultas-m-dicas

cmdkey /delete:git:https//github.com "tirar qualquer token que esteja cadastrado"
git config --local user.email "seu email"
git config --local user.name "seu nome"
git add . "para adicionar todas as alterações"
git commit -m "comentario" "<-pode ser qualquer comentario"
git push -u